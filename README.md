# set.py
This script uses Python sets to compare two friend groups and remove duplicates from a car list: it checks whether "John" and "Eric" are in the first set and whether both are in the second set, combines the two friend sets with union(), finds shared names with intersection(), shows names only in the first set via difference(), displays names...
